# Web app (same stack as z_profiler: FastAPI + HTMX + Jinja2)
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
jinja2>=3.1,<4
python-multipart>=0.0.9,<1
itsdangerous>=2.2,<3
python-dotenv>=1.0,<2

# Tender workflow
openpyxl>=3.1,<4          # build the pricing sheet (.xlsx) + read xlsx into the corpus
boto3>=1.34,<2            # S3 prompts + Bedrock Converse step-runner
requests>=2.31,<3         # generic HTTP AI provider (used when configured)
PyYAML>=6.0,<7            # shared project_data.yaml read/write
pdfplumber>=0.11,<1       # PDF → text for the cached document corpus
python-docx>=1.1,<2       # .docx → text for the cached document corpus
